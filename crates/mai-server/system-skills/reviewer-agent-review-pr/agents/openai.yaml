interface:
  display_name: Reviewer Agent — Review PR
  short_description: Script-assisted reviewer agent single-PR review with clone checkout, local tests, and inline comments
  default_prompt: As a reviewer agent, review only the target pull request named by Mai. Use the bundled helper, the gh sidecar, and the isolated agent workspace clone to prepare and validate that PR, then submit local validation results and inline GitHub comments.
policy:
  allow_implicit_invocation: false
