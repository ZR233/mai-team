interface:
  display_name: Reviewer Agent — Review PR
  short_description: Script-assisted reviewer agent single-PR review with clone checkout, local tests, and inline comments
  default_prompt: As a reviewer agent, use the bundled helper and raw GitHub MCP responses to select exactly one eligible open pull request in this repository, then review that PR with local validation and inline GitHub comments.
policy:
  allow_implicit_invocation: false
